// Contenu remplacé au build par src/data/digimonNames.loader.js (voir
// next.config.js) : { names, dubNames } calculés depuis le disque. Cette valeur
// vide n'est lue que si le module est chargé sans le loader.
module.exports = { names: [], dubNames: {} };
